{
  "name": "nishboard",
  "version": "0.1.0",
  "description": "Personal ambient desktop dashboard",
  "author": "Nish",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "pnpm --filter @dash/shared build && concurrently -k -n shared,server,renderer,main -c cyan,blue,green,yellow \"pnpm --filter @dash/shared dev\" \"pnpm --filter @dash/server dev\" \"pnpm --filter @dash/renderer dev\" \"pnpm --filter @dash/main dev\"",
    "build": "turbo build",
    "package": "pnpm build && node scripts/prepare-wincodesign.cjs && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --config electron-builder.yml",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron-builder": "^25.1.8",
    "turbo": "^2.3.3"
  }
}
